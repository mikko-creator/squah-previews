# elevatedental preview build.
# Disallow-all is DELIBERATE, not an oversight: this is an unfinished preview on a public URL, and it
# must not enter a search index while it carries representative market prices rather than the clinic's
# own figures.
#
# KNOWN LIMIT, stated plainly: this file is INERT here. Crawlers read robots.txt only at the ORIGIN
# root (https://mikko-creator.github.io/robots.txt), and this ships to a project subpath. The real
# protection is the per-page <meta name="robots" content="noindex"> on all 47 pages, which is live and
# verified. This file is kept so the launch floor is complete and so the intent is on the record.
User-agent: *
Disallow: /

Sitemap: https://mikko-creator.github.io/squah-previews/elevatedental/sitemap.xml
