<?xml version="1.0" encoding="UTF-8"?>
<!--
  DELIBERATELY EMPTY during the preview window. All 49 pages carry noindex, and listing a
  noindexed URL in a sitemap is a contradictory signal. Since robots.txt is inert at a project
  subpath, noindex is the ONLY working protection, so this file must not advertise the URLs.
  AT LAUNCH: drop noindex from each page META block and re-run the launch-floor generator.
  Pages held back right now: 49.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
</urlset>
